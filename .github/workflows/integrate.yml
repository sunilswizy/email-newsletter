name: Continues integration

on: 
  pull_request:
    branches: [ main ]

jobs: 
  test_pull_requests:
    run-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14
      - run: npm ci
      - run: npm run build
